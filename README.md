# Інформаційно-довідкова система "Глосарій-КБ"

Сучасна аналітична платформа для каталогізації, інтелектуальної обробки та пошуку термінів зі зв'язку, кібербезпеки та РЕБ. Розроблена для автономної роботи в закритій мережі з використанням локального ШІ.

## Архітектура

- **Frontend**: React + Vite + Tailwind CSS
- **Backend**: Node.js + Express + MySQL
- **AI**: Локальна LLM через Ollama (моделі `llama3` для аналізу та `nomic-embed-text` для семантичного пошуку)
- **Технології**: Server-Sent Events (SSE) для трансляції прогресу, векторні операції для семантичного пошуку.
- **Безпека**: Рольова модель доступу (RBAC), обов'язкове маркування грифів секретності (Відкрито, ДСК, Таємно), 100% локальне зберігання даних.

## Автентифікація та ролі

Система використовує JWT-токени (access token живе 15 хвилин, refresh token — 7 днів у httpOnly cookie).
При першому запуску міграцій автоматично створюється адміністратор: `admin@mitit.edu.ua` / пароль: `qwerty123` (хешується через bcrypt).

**Ролі користувачів:**
- `admin`: Повний доступ (керування термінами, документами, користувачами).
- `operator`: Право завантажувати документи та верифікувати знайдені ШІ терміни.
- `user`: Лише читання, базовий та AI-пошук.

Кожен користувач отримує доступ до термінів строго відповідно до свого персонального **Грифу секретності** (Public, DSP, Secret).

## Налаштування

1. **Встановіть MySQL та створіть базу даних:**
   ```bash
   mysql -u root -p
   CREATE DATABASE guid_book;
   EXIT;
   ```

2. **Встановіть схему БД:**
   ```bash
   mysql -u root -p guid_book < database/schema.sql
   ```

3. **Встановіть Ollama моделі:**
   ```bash
   ollama pull llama3
   ollama pull nomic-embed-text
   ```

4. **Змінні середовища (у файлі backend/.env):**
   ```
   DB_HOST=localhost
   DB_USER=root
   DB_PASSWORD=qwerty123
   DB_NAME=guid_book
   PORT=3001
   OLLAMA_URL=http://localhost:11434
   JWT_SECRET=your_jwt_secret_here
   JWT_REFRESH_SECRET=your_jwt_refresh_secret_here
   ```

## Запуск

### Backend
```bash
cd backend
npm install
npm run dev
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

## Функціонал

- Завантаження PDF/DOCX з обов'язковою перевіркою грифа обмеження
- Автоматичне видобування термінів за допомогою ШІ з верифікацією користувачем
- Базовий та семантичний пошук термінів
- Візуалізація джерел з прямими посиланнями на файли
- Контроль актуальності термінів
- Логування пошукових запитів

## Безпека

Система працює повністю локально, без зовнішніх API. Всі дані зберігаються в локальній БД з підтримкою грифів обмеження доступу.